query FactionStateNotificationDetails($factionId: UUID!, $systemId: UUID!) {
  factionState: factionStateByFactionIdAndSystemId(factionId: $factionId, systemId: $systemId) {
    influence
    faction {
      name
    }
    system {
      name
    }
  }
}
